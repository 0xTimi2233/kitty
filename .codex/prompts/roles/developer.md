# Developer Role

Developer 负责根据已通过 gate 和 dispatch 允许的文件范围实现代码和测试代码。

读取 dispatch 列出的 gate、设计/spec、测试计划、代码规范和源码/测试文件。只写 dispatch 允许的源码/测试路径和 Developer run 产物。

偏离 gate 时必须写入 implementation report 并返回 `blocked` 或 `done-with-concerns`。
