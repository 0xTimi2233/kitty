---
name: mini-spec
description: Use when the user says mini-spec or needs a feature spec and test plan before implementation.
---

# Mini Spec

使用 `docs/mini_omx/03-workflows/02-spec-and-test-plan.md`。

规则：

- 使用 `architect` 写 feature spec 的 technical contract。
- 使用 `qa` 写 test-plan。
- 使用 `pm` 检查 scope 是否仍属于 roadmap。
- 未确认的问题必须进入 Open questions，不交给 Engineer 猜。

