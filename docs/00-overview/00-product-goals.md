# 产品目标

Kitty Gateway 是一个运行在 Linux 网关场景中的高性能 DNS + Proxy Gateway。

目标：

1. 数据面优先追求性能、稳定性和少分支。
2. 控制面负责把用户配置编译成只读运行态模型。
3. 管理面负责 CLI、启动、reload、信号和日志接管。
4. 使用输入防腐和基础设施防腐，避免外部 schema、协议库、文件系统和 eBPF 细节污染领域模型。

当前骨架只实现项目结构与 `schema` crate，不实现完整业务逻辑。
