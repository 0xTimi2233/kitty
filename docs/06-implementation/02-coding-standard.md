# 编码规范

- 使用 Rust edition 2024。
- 生产代码禁止 `unwrap()`、`expect()`、`println!()`。
- 热路径避免无意义 clone、堆分配和字符串比较。
- 默认值、引用解析和优先级覆盖在编译期完成。
- runtime model 发布后只读。
- 异常信息和日志文本使用英文。
- 注释使用中文。
- 简单语义代码不加噪声注释。
- 工具函数必须有文档注释。
- 重复字段通过宏或 helper 生成。
- 测试代码不放在 `src/` 下。
